# Session 2026-05-21 09:23

> When to read: wenn Template-Update-Prozess, Sandbox-Bypass-Mechanismus oder das Branding-Commit nachvollzogen werden sollen.

## Kontext

Zwei unabhängige Aufgaben: (1) Template-Update via `/project:template-update`, (2) separater Commit für offene Projektänderungen aus der Vorsession.

## Was passiert ist

**Template-Update (`chore: sync template files v1.5.0 → v1.5.0 patch`)**
- Dry-run via `python3 bin/template-update.py --check` — Sandbox-Bypass nötig, da Template-Quelle außerhalb des Projektordners liegt
- 6 Dateien auto-adopted: `bootstrap.md`, `template-update.md`, `CHANGELOG.md`, `bin/bootstrap-mechanical.sh`, `docs/template-versioning.md`, `memory/template_baseline.md`
- 2 Dateien behalten (user-modifiziert): `.claude/settings.json`, `.github/workflows/check.yml`
- 3 divergente Dateien: `check-sandbox-bypass.sh` (kept — nur extra Kommentar), `session-start.sh` (adopted — Template-Namensmuster `_template-base-*` → `_claude-tmpl-*`), `tests/hooks.sh` (kept — bereits identisch mit Template)
- 73/73 Hook-Tests grün, Pre-Commit bestanden

**Projektänderungen aus Vorsession (`feat: apply Opzelura branding and puls color palette`)**
- `public/logo.svg` gelöscht, `public/opzelura.svg` hinzugefügt
- `src/App.svelte`: puls CSS-Variablen ergänzt (`--header-height`, `--color-navy`, `--color-blue-teal`, `--color-light-blue`, `--color-blue`, `--color-coral`, `--color-warm-grey`)
- `src/components/Header.svelte`: Placeholder-Text durch SVG-Logo ersetzt, Theme-Toggle ausgeblendet
- `src/components/VoteScreen.svelte`: Button-Raster mit Rundungen, Brand-Farben, angepasstes Layout
- `src/lib/config.js`: Antwortoptionen mit vollem Vitiligo-Kontext ausgeschrieben
- `docs/todos.md`: Phase 5–7-TODOs als abgeschlossen markiert
- `docs/sessions/2026-05-20-2333.md` + `docs/template-bugs-prompt.md` committed

## Entscheidungen

- Sandbox-Bypass für Template-Update per User-Klick genehmigt (Einzel-Use, TTL 60 s, CLAUDE.md-konform)
- `session-start.sh` adopted (neues Template-Namensmuster), `check-sandbox-bypass.sh` und `tests/hooks.sh` behalten
- Template-Update und Projektänderungen in zwei getrennten Commits

## Offene Punkte

- Branch ist 4 Commits ahead of origin/main — Push noch ausstehend
- Visueller Smoke-Test des Prototyps im Browser noch nicht protokolliert

## Notizen

- Template-Update-Script braucht Lesezugriff auf den Template-Quellpfad (`90_dev/__claude_base/_claude-tmpl-base`) — liegt strukturell außerhalb der Sandbox, Bypass ist jedes Mal nötig.
